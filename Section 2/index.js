/* Global Environment */

var a = "Hello World!";

function b() {
  return "Hey, called me?";
}

// variable a and function b can be accessed in browser console as they are made available by JS to global context
// Remember - you don't need to use console.log() inside browser console
